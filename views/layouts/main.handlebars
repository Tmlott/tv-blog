<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
  <title>A Bad TV Sub-Reddit</title>
</head>

<body class="">
  <header class="container">
    <nav class="navbar navbar-light bg-dark row">
      <div class="container-fluid col-12">
        <span class="navbar-brand mb-0 h1 text-white"><a href="/"
            class="text-decoration-none text-white">TV4U</a></span>
        <div class="d-flex">
          <span class="navbar-brand mb-0 h1 text-white"><a href="/"
              class="text-decoration-none text-white">Home</a></span>

          {{#if loggedIn}}
          <span class="navbar-brand mb-0 h1 text-white">
              <a href="/post" class="text-decoration-none text-white">Create Tv</a>
          </span>
          <span class="navbar-brand mb-0 h1 text-white">
            <a href="/" class="text-decoration-none text-white"id="logout">Logout</a>
          </span>
          {{else}}
          <span class="navbar-brand mb-0 h1 text-white">
            <a href="/login" class="text-decoration-none text-white">Create Tv</a>
          </span>
          <span class="navbar-brand mb-0 h1 text-white">
            <a class="text-decoration-none text-white" href="/login">Login</a>
          </span>
          {{/if}}
        </div>
      </div>
      </div>
    </nav>
  </header>
  {{{body}}}
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
  {{!-- <script src="/javascript/main.js"></script> --}}
</body>

</html>